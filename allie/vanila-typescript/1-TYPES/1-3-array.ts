{
  //Array
  const fruits: string[] = ["🍎", "🍌"];
  const scores: Array<number> = [1, 3, 4];

  function printArray(fruits: readonly string[]) {}

  //Tuple
  // 다른 타입을 함께 가질 수 있는 자료구조
  
}